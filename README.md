# Face-Mask-Detection
This project implements a Face Mask Detection system using Convolutional Neural Networks (CNNs). It classifies facial images into with-mask and without-mask categories after preprocessing and normalization. The model achieves high accuracy and demonstrates the use of deep learning in computer vision for public safety and healthcare applications.
# 😷 Face Mask Detection Using CNN

## 📌 Project Description
This project implements a Face Mask Detection system using Convolutional Neural Networks (CNNs). It classifies facial images into with-mask and without-mask categories after preprocessing and normalization. The model achieves high accuracy and demonstrates the application of deep learning in computer vision for public safety and healthcare use cases.

---

## 🧠 Overview
The goal of this project is to build a deep learning model capable of detecting whether a person is wearing a face mask or not from an image. The system is trained on a labeled image dataset and evaluated on unseen data to ensure good generalization.

---

## 🛠️ Technologies Used
- Python  
- TensorFlow / Keras  
- NumPy  
- Pandas  
- OpenCV  
- Matplotlib  
- Scikit-learn  

---

## 📂 Dataset
- Images classified into two classes:
  - With Mask
  - Without Mask
- Images are resized and normalized before training.

---

## ⚙️ Model Architecture
- Convolutional layers with ReLU activation  
- MaxPooling layers  
- Fully connected (Dense) layers  
- Dropout for regularization  
- Sigmoid output layer for binary classification  

---

## 📈 Performance
- High training and validation accuracy  
- Good generalization on test data  

---

## 🚀 Applications
- Public safety monitoring  
- Healthcare compliance  
- Smart surveillance systems  
- Computer vision learning projects  

---

